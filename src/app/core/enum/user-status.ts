/**
 * Enumeration representing the status of a user (e.g., online or offline).
 * Use this enum to indicate the current status of a user within the application.
 */
export enum UserStatus {
    ONLINE = "ONLINE",
    OFFLINE = "OFFLINE"
}